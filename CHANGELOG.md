# Changelog

## 1.0.1

- Repaired the CLR `CustomAttribute` table ordering so Mono runtime reflection can find `[BepInPlugin]`.
- Set the public BepInEx plugin and release version to `1.0.1`.
- Kept the plugin GUID, display name, internal assembly identity, method IL, Harmony operations, and save-fix logic unchanged.
- Retained the external project and DLL name `CUCoreLib.KrokMP.SaveHotfix`.
